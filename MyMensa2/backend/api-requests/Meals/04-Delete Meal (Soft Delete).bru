meta {
  name: Delete Meal (Soft Delete)
  type: http
  seq: 4
}

delete {
  url: http://localhost:8081/api/meals/1
  body: none
  auth: none
}

tests {
  test("Status should be 204", function() {
    expect(res.status).to.equal(204);
  });
}
