meta {
  name: Create Meal With Invalid Data (400 Error)
  type: http
  seq: 2
}

post {
  url: http://localhost:8081/api/meals
  body: json
  auth: none
}

body:json {
  {
    "name": "",
    "description": "",
    "price": -5.0,
    "cost": -2.0
  }
}

tests {
  test("Status should be 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Response should contain error message", function() {
    expect(res.body).to.have.property('error');
  });
}
