meta {
  name: Invalid Date Format (400 Error)
  type: http
  seq: 3
}

get {
  url: http://localhost:8081/api/meal-plans?startDate=invalid-date&endDate=2025-01-17
  body: none
  auth: none
}

params:query {
  startDate: invalid-date
  endDate: 2025-01-17
}

tests {
  test("Status should be 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Response should contain error message", function() {
    expect(res.body).to.have.property('error');
  });
}
