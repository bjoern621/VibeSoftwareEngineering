meta {
  name: Purchase Ticket - Reservation Expired
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/orders
  body: json
  auth: none
}

body:json {
  {
    "reservationId": 2,
    "userId": "test_user"
  }
}

tests {
  test("Status code is 404", function() {
    expect(res.status).to.equal(404);
  });
  
  test("Response has error code", function() {
    expect(res.body.code).to.equal("INVALID_REQUEST");
  });
  
  test("Response has not found message", function() {
    expect(res.body.message).to.contain("nicht gefunden");
  });
}
