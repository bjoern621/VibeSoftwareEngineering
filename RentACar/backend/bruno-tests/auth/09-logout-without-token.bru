meta {
  name: 09-logout-without-token
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/kunden/logout
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

docs {
  # Logout ohne Token - Unauthorized

  Testet, dass ein Logout ohne Authorization-Header fehlschlägt.

  ## Erwartetes Ergebnis
  - Status: 401 Unauthorized (JwtAuthenticationEntryPoint für fehlende Authentifizierung)
  - Kein Token wird zur Blacklist hinzugefügt

  ## Hinweis
  - Backend gibt 401 für fehlende/ungültige Authentifizierung zurück (403 nur für Autorisierung)
}

