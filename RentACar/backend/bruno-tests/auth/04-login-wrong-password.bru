meta {
  name: 04 - Login Wrong Password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/kunden/login
  body: json
  auth: none
}

body:json {
  {
    "email": "test.customer@example.com",
    "password": "WrongPassword!"
  }
}

tests {
  test("Status is 401 or 500 (auth error)", function() {
    expect([401, 500]).to.include(res.status);
  });
}
