meta {
  name: 02 - Get Vehicle Type COMPACT_CAR
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/vehicle-types/COMPACT_CAR
  body: none
  auth: none
}

tests {
  test("Status is 200 OK", function() {
    expect(res.status).to.equal(200);
  });

  test("Response contains correct vehicle type", function() {
    expect(res.body.name).to.equal("COMPACT_CAR");
  });
}
