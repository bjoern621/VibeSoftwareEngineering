meta {
  name: 01 - Calculate Price
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/buchungen/preis-berechnen
  body: json
  auth: none
}

body:json {
  {
    "vehicleType": "COMPACT_CAR",
    "pickupDateTime": "2025-12-15T10:00:00",
    "returnDateTime": "2025-12-20T10:00:00",
    "additionalServices": []
  }
}

tests {
  test("Status is 200 OK", function() {
    expect(res.status).to.equal(200);
  });

  test("Response contains price", function() {
    expect(res.body).to.have.property("totalPrice");
    expect(res.body.totalPrice).to.be.a("number");
  });
}
